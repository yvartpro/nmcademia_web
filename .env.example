# Dev: API origin when Vite runs on :5173 (proxies /api and /uploads)
VITE_BACKEND_ORIGIN=http://localhost:3000

# Should match api SITE_URL in production (often same as your public domain)
# VITE_BACKEND_ORIGIN=https://yourdomain.com
